import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Image Upscaler – Free 4x AI Image Enhancer | Sugesto',
  description:
    'Upscale and enhance images 4x for free. Upload any photo and download a high-resolution version instantly — no registration, no watermark.',
  keywords: ['image upscaler', '4x upscale', 'super resolution', 'enhance image', 'free image upscaler', 'image enlarger'],
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
